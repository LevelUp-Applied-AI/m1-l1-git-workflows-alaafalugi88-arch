# Testing Requirements
All changes must pass `python test_environment.py` before committing.

# Secrets Policy
Do not include API keys, passwords, or .env files in commits.

# Scope Boundaries
Agents can edit files in src/ and notebooks/.
Do not modify requirements.txt without review.

# Reproducibility Standard
All changes must run locally before being committed.